services:

  backend:
    build: ./backend
    container_name: traverse-backend
    env_file:
      - ./backend/.env
    ports:
      - "8000:8000"

  frontend:
    build: ./frontend
    container_name: traverse-frontend
    ports:
      - "5173:5173"
    depends_on:
      - backend